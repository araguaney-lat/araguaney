# ── Core ─────────────────────────────────────────────────────────────────────
# Versions tracked against the bioflow production app (battle-tested).
fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
pydantic[email]==2.13.4
pydantic-settings==2.15.0
PyJWT==2.13.0
bcrypt==4.3.0
python-multipart==0.0.32
requests==2.34.2
resend==2.36.0
svix==1.99.1
httpx==0.28.1
user-agents==2.2.0
slowapi==0.1.10
jinja2==3.1.6
alembic==1.19.1
sentry-sdk[fastapi]>=2.68.0

# ── Background jobs + cache ───────────────────────────────────────────────────
# Both degrade gracefully: with no REDIS_URL the cache is a no-op and queued
# tasks fall back to in-process FastAPI BackgroundTasks. See app/arq_pool.py.
arq==0.28.0          # durable async task queue backed by Redis
redis==5.3.1         # Redis client (cache + arq transport)

# ── Crypto — Fernet symmetric encryption for sensitive DB-stored values ───────
cryptography>=42.0.8

# ── Optional layers ───────────────────────────────────────────────────────────
# Uncomment the section(s) you need. See docs/optional-layers.md for setup.

# Cloudinary — image / video uploads (avatars)
cloudinary==1.45.0

# AI / OpenAI-compatible providers (OpenAI, DeepSeek, Groq, etc.)
openai==1.109.1

# Langfuse — LLM observability (traces, evals, prompts)
# langfuse==2.26.0

# Stripe — payments, subscriptions, webhooks
# stripe==11.2.0

# 2FA / TOTP — authenticator app codes + backup codes
pyotp==2.10.0

# QR codes + PDF label generation
qrcode[pil]==8.2
reportlab==4.5.1

# IFRC packing list Excel export
openpyxl==3.1.5

# Manifest PDF (HTML → PDF via WeasyPrint + Jinja2 is already above)
# Dockerfile system deps required: apt-get install -y libpango-1.0-0 libpangocairo-1.0-0 libcairo2 libgif-dev fonts-liberation
weasyprint==69.0

# GeoIP — MaxMind GeoLite2 country/city lookup
# geoip2==4.8.0

# ElevenLabs — AI voice generation
# elevenlabs==1.9.0

# Cloudflare R2 / S3-compatible storage (messaging attachments)
boto3==1.43.71
